# cs61c-labs


**你的新工作流程变得无比清晰：**

* **编译 **load_exit.s**:**

  ** code **Bash
* ```
     make TARGET=load_exit

  ```

```
* **运行 **load_exit**:**

  ** code **Bash

*  ```
      make TARGET=load_exit run
  
```

  **(它会先确保**load_exit**已编译，然后再运行它)**

* **清理 **load_exit**:**

  ** code **Bash
* ```
     make TARGET=load_exit clean

  ```

```
* **未来，你要处理一个新文件 **2_arithmetic.s**:**

  * **编译: **make TARGET=2_arithmetic
  * **运行: **make TARGET=2_arithmetic run
```
